#!groovy

pipeline {
    agent 'any'

    stages {
        stage('Jenkinsfile Test') {
            steps {
                script {
                    echo 'Test jenkinsfile patch -1'
                }
            }
        }
    }
}